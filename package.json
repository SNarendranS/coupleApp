{
  "name": "couple-app",
  "version": "1.0.0",
  "private": true,
  "description": "Production-Ready Realtime Couples Web Application",
  "scripts": {
    "postinstall": "npm --prefix shared install && npm --prefix server install && npm --prefix client install",
    "install:all": "npm install && npm --prefix shared install && npm --prefix server install && npm --prefix client install",
    "build:shared": "npm --prefix shared run build",
    "build:server": "npm --prefix server run build",
    "build:client": "npm --prefix client run build",
    "build": "npm run build:shared && npm --prefix client run build && npm --prefix server run build",
    "start": "node server/dist/server.js",
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"blue,magenta\" \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "test:server": "npm --prefix server run test",
    "test:client": "npm --prefix client run test",
    "test": "npm run test:server",
    "typecheck": "npm --prefix shared run typecheck && npm --prefix server run typecheck && npm --prefix client run typecheck"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  }
}
